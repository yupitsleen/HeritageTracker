# Example Environment Variables for Heritage Tracker
# Copy this to .env.development or .env.production and configure

# ==============================================================================
# Backend Mode Configuration (Choose ONE)
# ==============================================================================

# Mode 1: Mock API (Default - Fastest Development)
# - No database or backend server required
# - Uses in-memory mock data (45 sites)
# - Perfect for quick development and testing
VITE_USE_MOCK_API=true
VITE_USE_LOCAL_BACKEND=false

# Mode 2: Local Backend (Realistic Development)
# - Requires Docker Desktop running
# - Uses local PostgreSQL database + Express server
# - Run: npm run db:setup (one-time setup)
# - Run: npm run server:dev (start backend)
# VITE_USE_MOCK_API=false
# VITE_USE_LOCAL_BACKEND=true
# VITE_API_URL=http://localhost:5000/api

# Mode 3: Supabase Cloud (Production)
# - Uses Supabase hosted database
# - Configure Supabase project first
# VITE_USE_MOCK_API=false
# VITE_USE_LOCAL_BACKEND=false
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ==============================================================================
# Database Configuration (for Local Backend only)
# ==============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=heritage_tracker
DB_USER=heritage_user
DB_PASSWORD=heritage_dev_password

# ==============================================================================
# Server Configuration (for Local Backend only)
# ==============================================================================
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
