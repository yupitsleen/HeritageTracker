# Development Environment Variables for Heritage Tracker

# Backend Mode: Mock API (fastest, no database required)
VITE_USE_MOCK_API=true
VITE_USE_LOCAL_BACKEND=false

# To use local backend instead:
# 1. Start database: npm run db:setup
# 2. Start backend: npm run server:dev
# 3. Update:
#    VITE_USE_MOCK_API=false
#    VITE_USE_LOCAL_BACKEND=true

# API Configuration
VITE_API_URL=http://localhost:5000/api

# Database Configuration (for local backend)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=heritage_tracker
DB_USER=heritage_user
DB_PASSWORD=heritage_dev_password

# Server Configuration (for local backend)
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
